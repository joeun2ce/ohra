[project]
name = "query2answer"
version = "0.1.0"
description = "RAG query to answer pipeline"
requires-python = ">=3.12"
dependencies = [
    "jupyter>=1.0.0",
    "openai>=1.0.0",
    "qdrant-client>=1.16.0",
    "requests>=2.31.0",
]
