[project]
name = "document2embedding"
version = "0.1.0"
description = "Notion document to embedding pipeline"
requires-python = ">=3.12"
dependencies = [
    "jupyter>=1.0.0",
    "qdrant-client>=1.7.0",
    "notion-client>=2.2.1",
    "requests>=2.31.0",
]
